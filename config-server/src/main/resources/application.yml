server:
  port: 8889
  
spring:
  profiles:    active: native
    
  cloud:    config:
      server:
        #bootstrap: false
        native:          search-locations: E:/DEV/config
          
management:
  endpoints:
    web:
      exposure:
        # 모든 endpoints 를 보이도록 활성화 시킴
        include:
        - '*'          